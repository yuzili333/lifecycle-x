# 存续期数据探针智能体 MVP 定义

## 1. MVP 目标

存续期数据探针智能体 MVP 面向银行贷后管理人员、后续尽职调查人员、业务分析人员和数据支持人员，优先验证“业务人员通过自然语言完成数据提取、数据清洗、数据加工、数据分析和分析材料生成”的核心价值。

MVP 不追求一次性覆盖所有贷后管理场景，也不由智能体直接输出风险信号、风险等级或处置建议。首期先聚焦高频、高价值、数据结构相对明确的数据处理与探索分析场景，验证以下假设：

1. 业务人员无需编写 SQL 或 Python，即可发起数据分析任务。
2. 系统能够从导入数据或连接数据源中识别关键字段和分析口径。
3. 系统输出的数据结果必须带有来源、口径和加工过程，能够被业务人员引用和复核。
4. 系统能够支持多轮追问和局部调整，而不是只输出一次性固定报表。
5. 系统目标能够生成可归档的 Markdown / PDF / JSON 结构化结果；当前交付状态在 3.3 节单独说明。

### 1.1 MVP 价值主张

MVP 要验证的不是“AI 能否写一份看起来完整的报告”，而是“业务人员能否在不直接编写 SQL/Python 的情况下，通过受控工具链得到真实、可复核、可继续加工的数据结果”。首期价值集中在效率、技术门槛、口径一致性、结果可追溯、多轮探索和报告材料规范性六个方面。

### 1.2 MVP 产品原则

- 真实数据优先：无可用数据时返回缺失信息，不使用模拟数据或样例行形成结论。
- 计划与执行分离：模型规划任务，SQL/Python/图表/报告工具产生权威结果。
- 人工审批与复核：高风险或受控工具调用需要审批，数据集和待复核事项由用户确认。
- 血缘贯穿：SQL 结果、Workflow Dataset、Python 分析、图表和报告通过 Artifact 依赖关系串联。
- 风险边界：可以统计已有风险分类字段，但不自动生成风险等级、预警结论或处置建议。

## 2. MVP 用户与场景

### 2.1 目标用户

| 用户类型 | 主要诉求 | MVP 覆盖程度 |
|---|---|---|
| 贷后管理人员 | 快速整理客户经营、回款、资金流向和担保相关数据 | 覆盖数据查询、加工和报告初稿 |
| 后续尽职调查人员 | 归集尽调材料、查看数据来源和分析口径 | 覆盖数据血缘和分析材料生成 |
| 风险管理人员 | 查看业务人员提交的数据分析依据 | 覆盖结构化数据结果和过程 trace |
| 数据分析/科技人员 | 降低临时取数和专项分析支持压力 | 覆盖 CSV/数据库接入的基础链路 |
| 审核/管理人员 | 复核报告依据与过程 trace | 覆盖归档和执行 trace 的基础能力 |

### 2.2 首期业务场景

MVP 首期只覆盖以下四类场景：

1. 资金流向数据整理：提取放款后账户流水、交易对手、交易摘要、金额和时间序列，形成可筛选明细和汇总结果。
2. 回款账户变化分析：按周期统计回款金额、回款频率、主要交易对手、资金留存时间等事实性指标。
3. 外部事项数据整理：对司法、工商、行政处罚、舆情等外部事项进行结构化归集和时间线整理，不自动判断风险等级。
4. 后续尽调材料初稿生成：根据数据来源、加工过程、统计口径和分析结果生成可复核的材料初稿。

## 3. MVP 范围

### 3.1 必须包含

| 模块 | MVP 能力 |
|---|---|
| 自然语言任务入口 | 支持输入分析目标、客户名称、时间范围、输出格式等自然语言需求 |
| 数据源导入 | 支持持久 CSV 与会话临时 CSV；持久 CSV 需同时提供表字典；数据库连接作为可选验证能力 |
| 数据结构识别 | 识别字段名称、字段类型、金额字段、日期字段、客户字段、交易对手字段 |
| 分析计划生成 | 将用户意图拆解为数据选择、清洗规则、加工步骤、统计口径和输出物 |
| 数据清洗加工 | 支持字段映射、类型转换、缺失值提示、重复记录处理、分类汇总和周期对比 |
| 数据血缘生成 | 每项结果必须关联数据来源、记录范围、加工步骤和统计口径 |
| 多轮追问 | 支持基于上一轮结果调整时间范围、筛选客户、拆分维度、局部纠错 |
| 报告输出 | MVP 目标支持 Markdown、JSON、PDF；当前版本已实现 Markdown 报告 Artifact，JSON/PDF 文件导出待实现 |
| 归档引用 | 支持保存分析结果、报告、数据血缘和执行 trace |
| 异常处理 | 输入缺失、数据源失败、字段缺失、结果冲突、模型输出非法时返回结构化错误 |

### 3.2 暂不包含

| 暂不包含能力 | 说明 |
|---|---|
| 风险信号识别与风险等级判断 | 首期不由智能体输出风险信号、风险等级、预警结论或处置建议 |
| 自动发起审批或处置流程 | 不直接触发授信调整、提前收贷、风险处置等正式流程 |
| 全量外部数据实时采集 | 工商、司法、舆情等外部接口首期可通过导入数据验证 |
| 复杂机器学习模型训练 | 首期以数据加工、统计口径和可解释分析为主 |
| 多机构权限体系 | 首期只验证单机构或单租户使用流程 |
| 移动端 | 首期只覆盖桌面端操作台 |

### 3.3 当前版本交付状态

状态依据 2026-07-23 仓库代码与测试，不以“存在接口或测试桩”代替业务验收。

| MVP 能力 | 状态 | 当前实现 | 仍需完成 |
|---|---|---|---|
| 桌面工作台与登录 | 已实现 | Electron 工作台、密码/SSO 登录流程、会话刷新、角色权限、个人设置、明暗主题 | 银行统一身份与权限体系联调 |
| 自然语言会话 | 已实现 | 会话增删改、消息持久化、流式输出、重试、取消、运行进度和上下文保留 | 真实试点用户可用性验证 |
| CSV 导入与字段上下文 | 已实现 | 持久 CSV+表字典校验、临时会话 CSV、字段选择、Schema 上下文、预览/重命名/删除 | 业务字段字典和样例数据补齐 |
| 数据库接入 | 部分实现 | 连接配置、测试、元数据浏览界面和接口已存在 | 当前 MySQL 适配器为模拟实现，需接入真实只读数据库并验证权限/审计 |
| 分析计划与工具编排 | 已实现 | 任务路由、计划、依赖解析、执行状态、失败/取消、工具结果选择和 Artifact 指针 | 按业务场景验证计划准确率 |
| 受控 SQL | 已实现 | 只读校验、表列权限、审批、二次校验、脱敏、行数/超时限制、取消和审计 | 结合真实数据库完成端到端验收 |
| 受控 Python | 已实现 | 仅消费受控数据集，支持脚本安全校验、权限、审批、沙箱、超时、结果 Artifact 和审计 | 在目标银行运行环境验证依赖与资源限额 |
| 图表 | 已实现 | 支持 KPI、常见统计图、时间线、网络和表格等受控可视化及报告内引用 | 业务模板、导出图片和大数据量体验验证 |
| 报告 | 部分实现 | Markdown 报告 Artifact、版本、流式报告卡片、图表引用、查看与复制 | PDF/JSON 文件导出、正式报告模板和导出审计 |
| 血缘、版本与 Memory | 部分实现 | Tool/Artifact/Workflow Dataset 父子关系、版本、trace、本地 Memory 检索压缩和敏感过滤 | 独立血缘库、报告库、审计页面及完整版本差异体验 |
| 业务场景模板 | 部分实现 | 通用链路可执行查询、分析、绘图和报告；已预置“整体风险分类分布”Skill | 资金流向、回款、担保、外部事项和尽调报告需固化口径并验收 |

## 4. MVP 核心用户流程

1. 业务人员选择或导入数据源。
2. 持久 CSV 同时提供表字典并完成校验，或在当前会话附加临时 CSV；系统展示字段、类型、样例和可用字段上下文。
3. 业务人员输入任务，例如：“提取 XX 公司近三个月放款后资金流向，按交易对手和交易摘要汇总，并生成尽调材料摘要。”
4. 系统生成分析计划，列出将使用的数据源、筛选条件、清洗规则、加工步骤和统计口径。
5. 业务人员确认数据集，并按权限策略审批或拒绝 SQL/Python 工具调用。
6. 系统执行分析并输出明细数据、汇总指标、数据血缘、图表和报告初稿。
7. 业务人员继续追问，例如“把时间范围扩展到半年”“按交易对手拆分”“剔除工资类交易”。
8. 系统基于上下文重新分析并保留版本记录。
9. 当前版本可查看、复制并在会话中归档 Markdown 报告 Artifact；PDF/JSON 文件导出在后续版本完成。

## 5. MVP 成功标准

| 指标 | 验收标准 |
|---|---|
| 任务完成效率 | 典型资金流向整理任务从导入数据到报告初稿不超过 10 分钟 |
| 数据可追溯 | 100% 分析结果包含数据来源、记录范围和加工步骤 |
| 口径明确性 | 100% 指标包含统计口径、时间范围和筛选条件 |
| 异常可解释 | 字段缺失、数据源失败、结果冲突均返回结构化原因和补救建议 |
| 报告可用性 | 业务人员可基于报告初稿进行人工修订和归档 |
| 多轮可用性 | 至少支持 3 轮连续追问，且能引用上一轮结果 |

上述成功标准是业务验收目标。当前自动化测试证明了组件和流程约束，但尚未证明“10 分钟”“100% 可追溯”或“至少 3 轮”在真实试点数据和用户任务下达标，仍需使用正式样例数据集执行端到端验收。

## 6. 首期样例数据要求

| 数据对象 | 关键字段 |
|---|---|
| 客户信息 | 客户编号、客户名称、统一社会信用代码、行业、实际控制人、关联企业 |
| 授信合同 | 贷款编号、贷款金额、贷款用途、放款日期、到期日期、还款方式、合同约束 |
| 账户流水 | 客户编号、账户号、交易日期、交易金额、借贷方向、交易对手、交易摘要、交易后余额 |
| 交易对手关系 | 交易对手名称、关系类型、是否关联方、是否个人账户、来源依据 |
| 外部事项 | 客户编号、事项类型、发生日期、事项描述、来源、影响范围 |
| 担保信息 | 押品编号、押品类型、估值、登记状态、查封冻结状态、担保覆盖率 |

## 7. MVP 风险与约束

1. 数据质量不足会影响分析结果，系统必须提示字段缺失和口径限制。
2. 大模型可能生成不可靠表述，报告生成必须只引用结构化分析结果和数据血缘。
3. 银行业务场景和口径存在机构差异，筛选条件、字段映射和统计口径需配置化。
4. 涉及客户、账户、资金流水等敏感数据，必须按银行内部数据安全规范处理。
5. MVP 输出只能作为数据分析材料，风险研判、业务定性和处置动作由业务人员完成。

## 8. MVP 交付物

1. 桌面端业务工作台原型（已实现）。
2. 持久/临时 CSV 数据源导入与 Schema 上下文（已实现）。
3. 自然语言任务到 Agent/Tool 分析计划的转换能力（已实现，待业务准确率验收）。
4. 受控 SQL、Python、图表和 Markdown 报告链路（已实现）。
5. 结构化工具结果、Artifact 血缘、版本、trace 和本地 Memory（已实现基础能力）。
6. 真实只读数据库接入（当前未完成，现有 MySQL 适配器为模拟实现）。
7. PDF/JSON 文件导出与导出审计（当前未完成）。
8. 首批业务场景口径、业务验收样例数据集、试点反馈表和问题清单（待业务方提供并验收）。
