你是一名资深 Agent 开发工程师。

请使用 Goal 模式完成以下子任务。

【当前子任务】
实现 / 设计 / 编写：【数据管理模块改造】

【任务目标】
本轮只需要完成：
1. 将“数据库连接”和“CSV数据集”两个“TreeList”组件节点拆分为两个路由页面,保持与`TopNav`的菜单“DataSource”中的"Database"和“CSV”匹配,同时将菜单项中的图标大小调整为“24px”。
2. 工作台主界面顶部菜单“DataSource”中的菜单项“CSV”点击后导航到拆分后的“CSV”页面。
3. 菜单“DataSource”和"CSV"页面
 - 保留“Treelist”和“Table”组件展示，左侧“TreeList”,右侧数据“Table”。连接数据库的“TreeList”组件数据为空时，“Table”容器增加“EmptyState”组件，图标显示“database-source.svg“，标题显示“新增数据库连接”，副标题显示“空空如也的数据库”，提供“新增连接”按钮。“导入CSV”的“TreeList”组件数据为空时，“Table”容器增加“EmptyState”组件，图标显示“csv.svg“，标题显示“新增CSV数据”，副标题显示“空空如也的数据集”，提供“导入CSV”按钮。

 “EmptyState”组件代码示例：
  ```tsx
  <EmptyState
      icon={<Icon icon={InboxIcon} size="lg" />}
      title="No notifications"
      description="You're all caught up. New notifications will appear here."
      actions={
        <HStack gap={2}>
          <Button label="Settings" variant="secondary" size="sm" />
          <Button label="Refresh" variant="primary" size="sm" />
        </HStack>
      }
      isCompact
    />
  ```

 - 删除原页面顶部的工具栏容器，将原工具栏中的“新增数据库连接”和“导入CSV”按钮分别移动到“TreeList”组件根节点“我的连接”和“CSV数据集”中右对齐的位置。
4. CSV页面中的“TreeList”组件的每个“item”增加“ContextMenu”组件，支持用户删除手动导入的CSV数据；同时每个“item”不再默认定义为“数据库”，通过CSV导入的数据默认是一张表及对应的数据，点击后在右侧“Table”组件中直接展示表字段和数据。
5. “TreeList”组件根节点“CSV数据集”左侧不展示“CSV”图标。
6. 导入CSV弹窗新增“取消”按钮，点击后关闭弹窗，同时支持点击弹窗遮罩层关闭弹窗；导入弹窗不在提供手动输入`csv`数据内容的交互控件，更新为上传`CSV`文件的交互控件。

【上下文】
该子任务属于【数据管理】中的一部分。

【注意事项】
不明确的功能改造不要擅自修改，列出不明确的改造点清单。
