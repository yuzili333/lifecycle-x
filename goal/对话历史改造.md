你是一名资深 Agent 开发工程师。

请使用 Goal 模式完成以下子任务。

【当前子任务】
实现 / 设计 / 编写：【对话历史改造】

【任务目标】
本轮只需要完成：
1. 历史对话记录列表中删除文字“历史记录”以及“新建对话”按钮。
2. 在历史对话记录容器顶部新增icon按钮用于“新增对话”。

- 按钮示例代码：

```tsx
<Button
  label="New Chat"
  variant="primary"
  icon={<Icon icon={PlusIcon} />
```

3. 每条对话记录容器右侧增加“编辑按钮”，支持修改“记录名称”，提供修改弹窗支持用户编辑“记录名称”，最大长度200个字符，弹窗提供“取消”和“保存”两个按钮。点击“取消”放弃修改并关闭弹窗，点击“保存”则更新本地数据后关闭弹窗。

- 编辑按钮示例代码：

```tsx
<Button
  label=""
  variant="secondary"
  icon={<Icon icon={PencilSquareIcon} />}
/>
```

4. 每条对话记录容器右侧增加“删除按钮”，支持修改“记录名称”，提供删除动作确认弹窗，标题为“请确认是否删除”，副标题为“删除后对话数据将无法恢复”，提供“取消”和“确认”两个按钮。点击“取消”放弃删除并关闭弹窗，点击“确认”则更新删除本地数据后关闭弹窗。

- 删除按钮示例代码：

```tsx
<Button
  label=""
  variant="destructive"
  icon={<Icon icon={TrashIcon} />}
/>
```

```

【上下文】
该子任务属于【AI Assistant】中的一部分。

【注意事项】
不明确的功能改造不要擅自修改，列出不明确的改造点清单。
