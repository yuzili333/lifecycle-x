## plan

你是一名资深 Agent 开发工程师 / 全栈架构师。

我正在开发一个【存续期业务数据探针智能体】。

请使用 Plan 模式工作：先进行模块分析和方案设计，不要直接写代码。

【模块背景】
模块用于：
- 【说明模块的业务用途】
“存续期数据探针智能体”工作台模块，用于提供用户与智能体交互的界面。

- 【说明目标用户或使用场景】
目标用户为已完成认证的用户。

- 【说明当前已有能力或现有限制】

无。

【核心目标】
本模块需要实现：
1. 用户管理，包括个人资料、设置、退出登录等功能；
  - 个人资料管理，包括查看和修改个人信息，如姓名、头像、联系方式等；
  - 设置管理，包括常规、外观、配置、个性化等功能；
    - 常规设置，包括语言、时区、通知等功能；
    - 外观设置，区分浅色主题和深色主题，以及主题中包括强调色、背景、前景、字体、代码字体、半透明侧边栏、对比度等功能；UI字号大小，代码字号大小，程序坞图标（默认、浅色、深度）；
    - 配置设置，包括大模型、模型APIKey、Skill、MCP等功能；
  - 退出登录；

2. 导航菜单，包括各个功能模块的导航链接，如数据源管理、数据助手等；

【输入信息】
系统可能接收以下输入：
无

【期望输出】
系统最终应输出：
- 展示用户工作台主界面
- 展示用户个人资料、设置、导航菜单等功能模块

【标准】
请按照以下标准设计方案：
1. 输出必须可被用户引用作为判断依据，禁止输出定义不明确、模糊不清的分析内容；
2. 模块职责必须清晰；
3. 数据流必须可追踪；
4. 异常处理必须明确；
5. 后续应方便拆分成多个 Goal 子任务；
6. 方案应适合工程落地，而不是只停留在概念描述。

【边界】
设计时必须遵守：
1. 技术栈限定为：【例如 TypeScript + Node.js + Electron + FastAPI】；
- 客户端开发语言：`Typescript`、`React`等;
- 客户端开发框架：`Electron`;
- 界面开发框架：`React`、`astryx`，工作台模块主体界面布局选择`astryx`模版`shell-side-nav`;
- 服务端开发语言：`Nodejs`；
- 服务端开发框架：`Express`、`FastAPI`等；;
2. 不允许使用：【不允许的库 / 服务 / 架构】；
3. 不改变已有的：【API / 数据结构 / 业务流程】；
4. 不处理以下范围：【明确排除项】；
5. 如果需求存在不确定性，请列出假设，不要擅自决定。

【异常处理要求】
请在方案中考虑：
1. 输入缺失时如何处理；
2. 数据源失败时如何处理；
3. 多数据源结果冲突时如何处理；
4. 模型输出不合法时如何处理；
5. 无法完成任务时如何返回结构化错误；
6. 如何记录 fallback / retry / conflict trace。

【请输出】
请按以下结构输出：

1. 需求理解
2. 核心业务流程
3. 每个模块的职责
4. 数据流设计
5. 输入 / 输出协议设计
6. 异常处理流程
7. 数据源降级策略
8. 关键风险点
9. 测试策略
10. 后续可拆分的 Goal 子任务列表

注意：
- 这一轮不要写代码；
- 不要直接实现功能；
- 重点是设计清楚登录模块边界、模块职责和执行流程。
